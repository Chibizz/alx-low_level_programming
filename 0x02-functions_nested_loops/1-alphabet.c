#include "main.h"

/**
 * print_alphabet - check the code for cohort 11 students.
 *
 * Return: Always o (Success)
 */

void print_alphabet(void)
{
	char let = 'a';

	while (a <= 'z')
	{
		_putchar(let);
		lat++;
	}
	_putchar(let);
}
