#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	char ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	

	return (0);
}
